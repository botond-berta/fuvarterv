node_modules
dist
.vite
.env
.env.local
.env.*.local

# Legacy local dev scaffold (superseded by the committed root project)
.devserver

# Helyi vizuális ellenőrző harness (szükség esetén újra létrehozható)
.preview
